import * as React from "react";
import Cards from "../cards";

type ContentProps = {
  //
};

const Content: React.FC<any> = () => {
  return <article>
    <Cards />
  </article>;
};

export default Content;
